{
  "name": "meteor-peer-npm-issue",
  "private": true,
  "scripts": {
    "preinstall": "./install-deps",
    "start": "meteor run",
    "test-package-one": "meteor test-packages --driver-package dispatch:mocha-browser one -p 4000"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "meteor-node-stubs": "~0.2.0"
  }
}
